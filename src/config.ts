export const config = {
  api_base_url: 'http://localhost:1337/api',
  api_url_image: 'http://localhost:1337',
  // auth_base_url: process.env.NEXT_MODE !== 'dev' ? '/auth' : '/auth',
  // token: process.env.access_token,
};


export default config;
