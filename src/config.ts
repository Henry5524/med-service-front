export const config = {
  api_base_url: 'https://my-project-cupf.onrender.com/api',
  // api_url_image: 'https://my-project-cupf.onrender.com',
  // auth_base_url: process.env.NEXT_MODE !== 'dev' ? '/auth' : '/auth',
  // token: process.env.access_token,
};


export default config;
